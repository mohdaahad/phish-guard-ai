# Email Configuration for Contact Form
# Configure these variables to enable email notifications

# Email service (e.g., gmail, outlook, custom SMTP)
EMAIL_SERVICE=gmail

# Sender email address (the email that will send the notifications)
EMAIL_USER=your-email@gmail.com

# Email password or app password (use app password if using Gmail)
# For Gmail: https://myaccount.google.com/apppasswords
EMAIL_PASSWORD=your-app-password

# Recipient email (where you want to receive contact form submissions)
# If not set, it will default to EMAIL_USER
ADMIN_EMAIL=admin@sol9x.com
